node_modules/
.next/
coverage/

# secrets — never commit
.env
.env*.local

# local database and build artefacts
*.db
*.db-journal
tsconfig.tsbuildinfo

# scratch
.devlog
.palog
_*.ts

.vercel
.env*

# Office lock files, created when a .pptx/.docx is open
~$*
